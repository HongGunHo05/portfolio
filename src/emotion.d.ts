import '@emotion/react'

declare module '@emotion/react' {
  export interface Theme {
    colors: {
      black: string
      white: string
      carrot: string
      gray: string
      gray2: string,
      gray3: string
    }
  }
}
